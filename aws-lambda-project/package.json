{
  "name": "aws-lambda-project",
  "version": "1.0.0",
  "description": "A project for AWS Lambda function with TypeScript",
  "main": "src/handler.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "aws cloudformation deploy --template-file template.yaml --stack-name aws-lambda-project --capabilities CAPABILITY_IAM",
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.1000.0"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "jest": "^26.0.0",
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}